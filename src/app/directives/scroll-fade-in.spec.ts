import { ScrollFadeIn } from './scroll-fade-in';

describe('ScrollFadeIn', () => {
  it('should create an instance', () => {
    const directive = new ScrollFadeIn();
    expect(directive).toBeTruthy();
  });
});
