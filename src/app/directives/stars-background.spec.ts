import { StarsBackground } from './stars-background';

describe('StarsBackground', () => {
  it('should create an instance', () => {
    const directive = new StarsBackground();
    expect(directive).toBeTruthy();
  });
});
